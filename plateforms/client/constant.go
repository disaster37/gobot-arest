package client

// ModeInput permit to set pin as input
const ModeInput = "i"

// ModeInputPullup permit to set pin as input pullup
const ModeInputPullup = "I"

// ModeOutput permit to set pin as output
const ModeOutput = "o"

// LevelHigh permit to set output with high level
const LevelHigh = 1

// LevelLow permit to set output with low level
const LevelLow = 0
